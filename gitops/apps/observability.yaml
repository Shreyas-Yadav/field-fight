apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: field-fight-observability
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Shreyas-Yadav/field-fight.git
    targetRevision: main
    path: k8s/helm/observability
    helm:
      releaseName: field-fight-observability
      valueFiles:
        - ../../../gitops/environments/dev/observability-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
